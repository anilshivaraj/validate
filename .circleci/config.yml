version: 2
jobs:
  build:
    machine: true
    working_directory: ~/ci_app
    
    steps:
      - checkout

      - run:
          name: Greeting
          #- echo Hello, world $PARAM1 . $PARAM1 == 'NoTi-5;E-Bikes;Pure-Aloe'
          command: | # Test multiline yaml support
            if [[ "$PARAM1" == "5" ]]; then
              echo PARAM1 is 5
            else
              echo PARAM1 is not 5
            fi

      - run:
          name: Print the Current Time
          command: date
