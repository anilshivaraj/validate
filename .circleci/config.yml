version: 2
jobs:
  build:
    machine: true
    working_directory: ~/ci_app
    
    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world $PARAM1 .

      - run:
          name: Print the Current Time
          command: date
