version: 2
jobs:
  build:
    machine: true
    working_directory: ~/ci_app
    
    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world $PARAM1 . $PARAM1 == 'NoTi-5;E-Bikes;Pure-Aloe' 
                  if [[ "${PARAM1}" == "NoTi-5;E-Bikes;Pure-Aloe" ]] ; then echo Hellllllll

      - run:
          name: Print the Current Time
          command: date
